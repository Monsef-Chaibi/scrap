# Environment Configuration Example
# Copy this file to .env and update values as needed
# Note: For Render.com deployment, PORT is automatically set

# Server Port (optional, defaults to 3000)
PORT=3000

# Node Environment
NODE_ENV=production

# Puppeteer Executable Path (optional, automatically set in Docker)
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
